# 贡献指南

## 基本流程

1. 从最新的 `main` 创建分支：`feature/<简短名称>`、`fix/<简短名称>` 或 `docs/<简短名称>`。
2. 保持一次提交只解决一类问题，提交信息使用 `type: summary`，例如 `fix: correct chassis velocity limit`。
3. 推送分支并创建 Pull Request；禁止在未经负责人同意时直接推送 `main`。
4. 确认能够编译、启动或通过相应测试，并在 PR 中写明验证方法。
5. 至少一名成员审核后使用 Squash merge；合并后删除功能分支。

## 提交前检查

- 不包含 token、密码、Wi-Fi 凭据、私钥、个人数据或内部服务器地址。
- 不提交 `build/`、`install/`、`log/`、IDE 缓存、固件产物和大型日志。
- 第三方代码、SDK、模型和比赛资料允许在当前仓库中分发。
- 配置中的机器人编号、IP、串口和标定值已有说明，不会误用于其他机器人。
- README、启动命令及依赖版本随代码同步更新。

## 分支与版本

`main` 仅保存经过审核的稳定代码。比赛现场版本请创建不可变标签，建议格式为 `season-round-robot-YYYYMMDD`。
